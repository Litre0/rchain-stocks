# Raw pipeline state: pools.json alone is 20 MB and would add ~1 GB/month of
# git history on a 30-minute cron. It lives in the Actions cache instead.
data/pools.json
data/tokens.json
data/snapshot.json
data/live.json
data/live_1h.json
data/stocks.json
dashboard.html
__pycache__/
*.pyc

# real identifiers for leakcheck.sh — never committed
.leakpatterns
